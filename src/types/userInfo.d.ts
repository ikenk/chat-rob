export type LoginInfo = {
  account: string;
  password: string;
};

export type RegisterInfo = {
  account: string;
  password: string;
  checkpass: string;
};

export type BaseInfo = {
  account: string;
  _id: string;
};
